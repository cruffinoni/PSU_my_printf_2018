/*
** EPITECH PROJECT, 2018
** handle_extraf.c
** File description:
** Handle flags below: +, - and #
*/

#include "./my_printf.h"
#include "./utils/utils.h"
/*
void handle_extraf(char const *src, int *index, t_local_spe *specifier)
{
    while (src[*index + 1] != '\0') {
        switch (src[*index + 1]) {
            case '-': specifier->flags_size |= 0b1;
                break;
            case '+': specifier->flags_size |= 0b10;
                break;
            case '#': specifier->flags_size |= 0b100;
                break;
            default: return;
        }
        *index = *index + 1;
    }
}*/